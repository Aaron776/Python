# Pip es un gestor de paquetes o paquetes, modulos o librerias lo que nos pemite compartir o utilizar liberias que tiene Python
#Pip ya iene instalado cuando instalamos Python en nuestro computador

#POdemos ver que version de pip tenemos en nuestro computador asi como tambien mediante comandos podemos instalar paquetes
#para que podamos utilizar por ejemplo: para que python trabaje con documentos word poenmos el comando: pip3 install python-docx
# y luego impratmos aqui en el ide

from docx import Document
document=Document()

#Ahora si queremos eliminar ese paquete o modulo que instalamos ponemos el comando: pip3 uninstall python-docx